# Build artifacts
dist/
*.zip

# Dependencies
node_modules/

# Local secrets (Cloudflare Worker)
backend/.dev.vars
.dev.vars
.env
.env.*

# Stan lokalny wranglera (cache, sesja) – nie należy do repo
backend/.wrangler/

# Lingvido to osobny projekt z własnym repozytorium git.
# Trzymany obok wyłącznie jako materiał źródłowy – nie wciągamy go tutaj.
lingvido/

# OS / editor
.DS_Store
Thumbs.db
*.log
